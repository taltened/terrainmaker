export interface Theme {
  /** CSS class for a control panel. */
  readonly panel: string;
  /** CSS class for interactive text. */
  readonly link: string;
  /** CSS class for interactive text. */
  readonly 'link-checked': string;
}
